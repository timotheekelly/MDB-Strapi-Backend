'use strict';

/**
 * component-variant service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::component-variant.component-variant');
