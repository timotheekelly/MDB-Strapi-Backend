'use strict';

/**
 * component-version service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::component-version.component-version');
